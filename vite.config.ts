import { defineConfig } from "vite";

export default defineConfig({
  // GitHub Pages project sites serve from /<repo>/. The default base of "/"
  // builds a page that 404s on every asset.
  base: "/fantasy-drafter/",
});
